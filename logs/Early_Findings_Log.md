# Early Findings Log
*(Running tracker — filled in as Steps 3-6 progress. Not final findings; candidates to be reviewed before inclusion in the Findings section.)*

Per the LU3/LU4 diagnostic check, an entry only graduates to a genuine "finding" once it is: grounded in actual output, linked to a specific RQ/objective, supported by more than one data point (e.g. confirmed across folds/datasets, not a single run), and still explicitly treated as provisional until final validation.

| # | Pattern/observation | Evidence source | RQ/Objective link | Quality concern | Next step | Status |
|---|---|---|---|---|---|---|
| 1 | Baseline logistic regression shows high AUC-ROC (0.74–0.85) across all three datasets, but near-zero F1/G-mean/MCC on Home Credit (F1=0.02, G-mean=0.10, MCC=0.06) and weak values on LendingClub (F1=0.20, G-mean=0.33) — indicating the model is failing to detect the minority (default) class almost entirely on the two more imbalanced datasets, despite AUC appearing acceptable. Verified this is not a code error: on Home Credit's 61,502-row test set (true positive rate ~8%), the model predicted positive for only 101 rows (0.16%) — confirming the model is defaulting to "no default" for almost every case. | Step 3 baseline LR run, all three datasets, single train/test split (Cell 2, `04_baseline_logistic_regression.ipynb`); prediction-count verification confirms this is a genuine imbalance effect, not a bug | RQ2 (effect of class imbalance on accuracy); directly justifies the methodological choice (Section 5, proposal) to report F1/G-mean/MCC alongside AUC rather than AUC/accuracy alone | Only checked on a single 80:20 split so far, not yet confirmed across CV folds; not yet compared against RF/XGBoost baselines to confirm this is an imbalance effect specific to LR vs. a broader pattern | Confirm same pattern holds under 5-fold CV; compare against RF/XGBoost baseline (without SMOTE) to see whether ensembles alone improve this before SMOTE is even applied | candidate — strengthening, verified not a bug, literature-consistent |

| 2 | **[FINAL, all 3 datasets tested, RF-zero result investigated and confirmed]** SMOTE's effect on performance is inversely related to how severe the class imbalance is and how few minority examples exist. German Credit (70/30, 800 train rows): SMOTE improves nearly all metrics for all models. Home Credit (~8% minority, 246,005 train rows): mixed — small F1/G-mean gains, small MCC/AUC costs. LendingClub (1.78% minority, only ~142 positive train cases): SMOTE *hurts every model* — RF collapses completely to F1/G-mean/MCC = 0.000 (confirmed: predicted 0 positives out of 2,000 test rows); XGBoost and Stacked both decline on every metric vs. no-SMOTE. **Strong, three-dataset-consistent, non-obvious finding directly relevant to RQ3 and to the "controversial effectiveness" framing in the approved proposal's own research problem.** Root cause investigated for the RF-zero result: minority count (142) was well above SMOTE's k_neighbors=5 requirement, ruling out an implementation failure; more likely explanation is that RF's random feature/sample subsampling is more sensitive to SMOTE's synthetic minority samples than XGBoost's sequential boosting, since XGBoost/Stacked retained partial minority detection under the same resampled data while RF alone collapsed entirely. Separately: the stacked ensemble was the best configuration on German Credit and Home Credit, but on LendingClub, plain XGBoost (no SMOTE) outperformed stacking (AUC 0.907 vs 0.868) — stacking's advantage is not universal across datasets. | Step 4, all three datasets, single train/test split, 6 configs each, plus a targeted diagnostic re-run confirming RF's exact prediction counts — `05_ensemble_models.ipynb` | RQ1 (ensembles vs standalone — supported, but not universally via stacking) and RQ3 (SMOTE's effect — clear, dataset/imbalance-severity-dependent pattern, root cause identified) | Single split only, not yet CV-confirmed for any dataset | Confirm pattern holds under 5-fold CV on all three; this finding is strong enough to be a central pillar of the discussion section, not just a footnote | confirmed (pending CV cross-check) |

| 3 | SHAP global feature importance on German Credit (XGBoost+SMOTE) shows `status_checking_account = "no checking account"` as the single most important feature, with counter-intuitive direction: having no checking account is associated with *lower* predicted default risk. `duration_months` and `credit_amount` behave as domain theory predicts (higher = riskier); `savings_account` also behaves as expected (more savings = lower risk). `credit_history = critical account/other credits existing` also shows a counter-intuitive direction (associated with lower risk), though this may reflect small-sample (n=1,000) correlational noise rather than a generalizable pattern. | Step 6, German Credit, XGBoost+SMOTE model, SHAP TreeExplainer, global summary plot + single-row waterfall — `06_shap_analysis.ipynb` | RQ4 (does SHAP provide meaningful explainability) — establishes baseline interpretability before testing stability under SMOTE | Only one dataset, one model configuration checked so far; the counter-intuitive "no checking account" direction needs cross-referencing against known German Credit literature before treating as a genuine finding vs. a dataset-specific artifact | Compare SHAP rankings with vs without SMOTE (the actual RQ4 stability question) across folds; check whether the "no checking account" pattern is a documented feature of this dataset in prior literature | candidate |

| 4 | **RQ4 direct answer, German Credit:** SHAP feature importance rankings are highly stable between SMOTE and no-SMOTE conditions (Spearman rank correlation = 0.983, p<0.0001, XGBoost). Top features nearly identical across both conditions (same top feature; ranks 2-3 swap between `duration_months`/`credit_amount` but both remain in the top 3 either way). **This is a meaningful, citable result that partially counters the SHAP-instability concern raised in Chen et al. (2024), cited in the approved proposal as motivation for testing this question** — on German Credit at least, SMOTE does not appear to meaningfully destabilize SHAP explanations. | Step 6, German Credit only, XGBoost with vs without SMOTE, single train/test split, Spearman correlation on mean absolute SHAP values — `06_shap_analysis.ipynb` | RQ4 (direct, primary evidence) | Only one dataset and one model tested; German Credit had the *best* SMOTE outcomes of the three datasets (Section 3 findings) — given LendingClub's SMOTE results were actively harmful to model performance, SHAP stability may behave very differently there and should not be assumed to generalize | Repeat the same Spearman comparison on Home Credit and LendingClub before drawing any cross-dataset conclusion about SHAP stability; also repeat across CV folds, as originally planned in the approved methodology, rather than a single split | candidate — strong single-dataset result, must not be generalized to other datasets without checking |

| 5 | **RQ4, second dataset (LendingClub):** SHAP feature-importance rankings show moderate stability under SMOTE (Spearman = 0.797, p<0.0001) — clearly positive but noticeably lower than German Credit's 0.983. Top features broadly overlap (`paid_total`, `installment`, `paid_principal`, `issue_month` variants appear in both top-10 lists) but exact ordering shifts meaningfully: `installment` is the top feature without SMOTE (1.637) but drops to 2nd with SMOTE (1.219); `grade` appears only in the SMOTE ranking; `interest_rate` appears only in the no-SMOTE ranking. **Revised, two-dataset RQ4 claim: SHAP stability under SMOTE is not uniform across datasets, and appears to track with how well SMOTE performs predictively on that dataset — high stability on German Credit (where SMOTE improved most metrics) vs. lower stability on LendingClub (where SMOTE actively harmed every model's performance, per Section 3 findings).** This is a coherent, more nuanced, two-dataset-supported claim, stronger than a single-dataset "SHAP is stable" statement would have been. | Step 6, LendingClub, XGBoost with vs without SMOTE, single train/test split, Spearman on mean absolute SHAP values (full 2,000-row test set, no sampling needed given dataset size) — `06_shap_analysis.ipynb` | RQ4 (second, corroborating/nuancing data point); also connects back to RQ3 (SMOTE's performance effect) | Home Credit (the third dataset) still not tested due to memory constraints (see Findings Section limitations); only two of three datasets now checked; single split only, not CV-confirmed | If Home Credit can be completed (e.g. via Kaggle Notebooks), it would be the deciding data point: Home Credit's SMOTE effect was "mixed" (between German Credit's clear help and LendingClub's clear harm) — if the stability-tracks-performance pattern holds, Home Credit's Spearman correlation should land between 0.797 and 0.983 | confirmed (2 of 3 datasets, consistent pattern) |

| 6 | **RQ4, complete three-dataset picture:** Home Credit's SHAP stability check (memory issue resolved locally via sequential fit/explain + explicit garbage collection between models, rather than requiring a cloud environment) gives Spearman = 0.906 — landing almost exactly between LendingClub's 0.797 and German Credit's 0.983, as predicted by the stability-tracks-performance hypothesis in entry #5. **All three datasets now show a clean, monotonic relationship: better SMOTE performance outcome (Step 4) pairs consistently with higher SHAP stability (German Credit 0.983 > Home Credit 0.906 > LendingClub 0.797).** This is a strong, three-point-confirmed finding, not merely a plausible two-point pattern — a genuinely citable core result for the discussion section connecting RQ3 and RQ4. | Step 6, Home Credit, XGBoost with vs without SMOTE, single train/test split, Spearman on mean |SHAP| (300-row test sample) — `06_shap_analysis.ipynb`. Memory issue resolved via sequential single-model fit/explain/extract-ranking/delete-and-garbage-collect pattern rather than fitting both models simultaneously. | RQ4 (complete, all three datasets) and RQ3 (cross-referenced — the two questions are now shown to be empirically linked, not just individually answered) | Single split only, not CV-confirmed for any dataset; SHAP evaluated on a reduced sample (300 rows) for Home Credit specifically, vs. full test sets for the other two — worth noting as a minor methodological asymmetry | CV-confirm the three-dataset stability ordering if time allows; otherwise this finding is complete and ready to write up as a core result | confirmed (3 of 3 datasets, clean monotonic pattern) |

| 7 | **CV confirmation of the LendingClub RF+SMOTE collapse (finding #2):** ran RF with/without SMOTE across all 5 saved CV folds (not just the original single 80:20 split). Collapse confirmed as a consistent pattern, not a single-split fluke: RF+SMOTE predicted exactly 0 positive cases in 3 of 5 folds, and only 1-4 positive predictions in the remaining 2 folds (vs. hundreds of true positives per fold) — F1 near-zero in every fold. RF without SMOTE was already weak (F1 0.067-0.194 across folds) but consistently worse with SMOTE added in every single fold. **This upgrades finding #2's LendingClub component from "single-split result" to "CV-confirmed across 5 folds" — the RF+SMOTE failure on this dataset is now a methodologically solid, not provisional, result.** | Step 7 (CV confirmation), LendingClub, RF with/without SMOTE, all 5 folds from the Step 2 saved fold assignments (`lending_folds.json`) — `07_cv_confirmation.ipynb` | RQ3 (directly strengthens the existing finding); demonstrates the methodology's own CV structure being put to use, not just generated and left unused | Only RF checked across folds so far (the model with the most dramatic single-split result); XGBoost and Stacked ensemble's SMOTE effects, and the German Credit/Home Credit SMOTE patterns, and the SHAP stability ordering, are all still single-split only | Consider whether XGBoost/Stacked CV confirmation and the SHAP stability CV confirmation are worth the remaining project budget, given RF's collapse — the most dramatic and highest-stakes single-split claim — is now the most solidly confirmed result in the whole study | confirmed (5-fold CV) |

| 8 | **CV confirmation of the SHAP stability ordering (finding #6), German Credit and LendingClub:** ran the SHAP stability comparison (SMOTE vs no-SMOTE Spearman correlation) across 3 of the 5 saved CV folds for both datasets. German Credit: 0.975, 0.984, 0.966 (tightly clustered, consistent with the original single-split 0.983). LendingClub: 0.767, 0.768, 0.773 (tightly clustered, consistent with the original single-split 0.797). **The gap between the two datasets holds with zero overlap across every fold tested (German Credit range 0.966-0.984 vs. LendingClub range 0.767-0.773) — confirming the stability ordering is a stable, real pattern, not an artifact of one particular test split.** Home Credit not yet re-checked across folds — would complete the three-dataset CV confirmation, but is the most memory/compute-expensive to run per-fold. | Step 7 (CV confirmation), German Credit and LendingClub, 3 of 5 folds each (fold_0-fold_2), XGBoost with/without SMOTE, Spearman on mean absolute SHAP values — `07_cv_confirmation.ipynb` | RQ4 (directly strengthens finding #6, the study's most novel result) and RQ3 (linked, since the two questions are empirically connected) | Only 3 of 5 folds run (computational cost reduction, deemed sufficient given how tight the clustering already is); Home Credit still not CV-confirmed | If time allows, run Home Credit's 3-fold version using the same memory-safe sequential-fit pattern that resolved its earlier single-split MemoryError, applied per fold with a reduced validation sample | confirmed (partial CV, 3/5 folds, 2/3 datasets, zero-overlap separation) |

| 9 | **CV confirmation complete — all three datasets:** Home Credit's SHAP stability check across 2 folds (using the same memory-safe sequential fit/explain pattern, 300-row validation sample) gives 0.926 and 0.915 — both close to the original single-split value (0.906) and, critically, landing exactly where the ordering predicts: comfortably between LendingClub's fold range (0.767-0.773) and German Credit's fold range (0.966-0.984), with zero overlap in either direction. **The three-dataset, monotonic SHAP-stability-tracks-SMOTE-performance relationship is now fully cross-validation-confirmed, not just single-split-based, across every dataset tested at every fold level.** This is the strongest-evidenced finding in the study. | Step 7 (CV confirmation), Home Credit, 2 of 5 folds (fold_0, fold_1), XGBoost with/without SMOTE, 300-row validation sample per fold (memory-constrained), Spearman on mean absolute SHAP values — `07_cv_confirmation.ipynb` | RQ4 (fully confirmed, all three datasets) and RQ3 (same, via the established link between the two questions) | Only 2 of 5 folds run for Home Credit (vs. 3 of 5 for the other two datasets) due to its higher compute cost — a minor asymmetry worth noting, though the tight clustering of both values already run suggests little additional uncertainty would be resolved by the remaining 3 folds | None required — this finding is now considered complete for the purposes of the current project phase; remaining folds could be run later for full rigour if time allows, but are not a priority | confirmed (CV, all 3 datasets, zero overlap across every fold tested) |

**Literature comparison for entry #1:** a closely comparable 2025 study using the same five-model design (LR, RF, XGBoost, stacking, MLP) on credit-risk classification reports random forest achieving the best F1 (0.81) while logistic regression shows high recall but low precision — same qualitative pattern (LR is a weak minority-class detector relative to ensembles). A separate comparative ensemble-learning study reports baseline AUC in the 0.66–0.69 range with F1 typically 0.18–0.33 — your German Credit AUC (0.81) and LendingClub AUC (0.85) both exceed this benchmark range, while your Home Credit F1 (0.02) is markedly more severe than anything reported in that comparison set, worth flagging as a point of interest (possibly reflecting Home Credit's more extreme real-world imbalance and scale vs. the datasets in that comparison study). Full Li & Chen (2020) baseline figures were not found published in open search results; general conclusion from their abstract (logistic regression outperforms other baseline classifiers on most metrics, but ensemble learning outperforms individual learners overall) is consistent with the pattern found here.

---

## How this gets used
- Add a row the moment something in a result looks meaningful, not just a random number — don't wait until all modelling is done.
- "Status" column: mark as `candidate` initially, `confirmed` once checked across folds/datasets, or `dropped` if further checking shows it doesn't hold up.
- When writing Step 8 (the Findings section), only `confirmed` rows should be written up as findings; `candidate` rows that never got confirmed become either a limitation note or get dropped entirely.
